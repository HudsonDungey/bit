{
  "name": "pulse",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w packages/sdk && npm run build -w packages/scheduler",
    "typecheck": "npm run typecheck -w packages/sdk && npm run typecheck -w packages/scheduler",
    "test:e2e": "node --loader ts-node/esm packages/scheduler/test/e2e.ts",
    "compile:contracts": "npx hardhat compile"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
